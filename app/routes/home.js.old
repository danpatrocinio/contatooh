//var controller = require('../controllers/home')(); --> substituida pela funcao do express-load

module.exports = function(app) {
	var controller = app.controllers.home;
 	app.get('/index', controller.index);
  	app.get('/', controller.index);
}